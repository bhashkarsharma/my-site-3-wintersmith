extends layout

block append vars
  - bodyclass = 'article-detail lab-detail'

block prepend title
  | #{ "Lab - "}

block header
  div.logo
    a(href=locals.url)
  h1.title
    a(href=locals.url)= locals.name
  p.description= locals.description

  h1= page.title

block content
  article.article
    section.content!= typogr(page.html).typogrify()
  
block prepend footer
  div.nav
    a(href=contents.index.url) « Back to home
